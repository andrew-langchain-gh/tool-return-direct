[project]
name = "tool-return-direct"
version = "0.1.0"
description = "Skipping the redundant ReAct review call for A2UI tools in LangGraph"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.14,<2",
    "langchain-anthropic>=1.5.4,<2",
    "langgraph>=1.2.10,<2",
    "langsmith>=0.10.17,<1",
    "python-dotenv>=1.0,<2",
]
